MegaZIP 026 — flutter_custom_tabs_android en JVM 17

Base exacta:
- salumed_app-main (5).zip

Error confirmado:
:flutter_custom_tabs_android:compileDebugKotlin
Java target = 17
Kotlin target = 11

Corrección:
Se añade exclusivamente:
project.name == "flutter_custom_tabs_android"

a la lista de módulos Kotlin que compilan con JVM 17.

JVM 17:
- app
- assets_audio_player
- audio_session
- audioplayers_android
- device_info_plus
- flutter_custom_tabs_android

JVM 11:
- file_picker
- demás plugins Android heredados que compilan Java 11

No se modifica:
- código Dart
- diseño
- pagos
- API de audio
- plugins locales
- applicationId
- compileSdk
- targetSdk

Aplicación:
1. Descomprimir encima de:
   F:\SALUMED\CODE\APP

2. Limpiar y compilar:

cd .\android
.\gradlew.bat --stop
cd ..

flutter clean
Remove-Item -Recurse -Force .\build -ErrorAction SilentlyContinue
Remove-Item -Recurse -Force .\.dart_tool -ErrorAction SilentlyContinue
Remove-Item -Recurse -Force .\android\.gradle -ErrorAction SilentlyContinue

flutter pub get
flutter build apk --debug -v *> build_026.log

Git:
git status
git add .
git commit -m "fix: align custom tabs Android Kotlin target to JVM 17"
git push
