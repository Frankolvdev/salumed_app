MegaZIP 035 — Facebook Android deshabilitado correctamente

Base exacta:
- salumed_app-main (11).zip

Se elimina únicamente de:
android/app/src/main/AndroidManifest.xml

Entradas retiradas:
- com.facebook.sdk.ApplicationId
- com.facebook.FacebookActivity
- com.facebook.CustomTabActivity
- @string/facebook_app_id
- @string/fb_login_protocol_scheme

Se conserva:
- flutter_login_facebook en pubspec.yaml
- imports y código Dart de Facebook
- Google Maps
- Firebase
- OneSignal
- pagos y deep links
- applicationId y diseño

Facebook queda sin integración nativa Android. Para reactivarlo después habrá
que restaurar esas entradas y configurar las credenciales correspondientes.

Aplicación:
1. Descomprimir encima de F:\SALUMED\CODE\APP
2. Compilar:

cd .\android
.\gradlew.bat --stop
cd ..

flutter clean
Remove-Item -Recurse -Force .\build -ErrorAction SilentlyContinue
Remove-Item -Recurse -Force .\.dart_tool -ErrorAction SilentlyContinue
Remove-Item -Recurse -Force .\android\.gradle -ErrorAction SilentlyContinue

flutter pub get
flutter build apk --debug -v *> build_035.log

Git:
git status
git add .
git commit -m "fix: disable unused Facebook Android manifest integration"
git push
