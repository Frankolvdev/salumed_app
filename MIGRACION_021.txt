MegaZIP 021 — Limpieza real de plugins locales

Base:
- salumed_app-main (2).zip, exactamente el proyecto actualizado enviado por el usuario.
- El log Pasted text(85).txt está incompleto: termina durante la configuración de audio_service,
  sin BUILD FAILED ni excepción final.

Correcciones incluidas:
1. flutter_datetime_picker:
   - bool showTitleActions: true -> bool showTitleActions = true
   - bool showSecondsColumn: true -> bool showSecondsColumn = true
   - locale: LocaleType.en -> LocaleType locale = LocaleType.en
   - Se conserva DatePickerTheme propio y todo el comportamiento.

2. timelines:
   - hashValues -> Object.hash
   - hashList -> Object.hashAll

3. assets_audio_player_web:
   - SDK Dart actualizado de <3.0.0 a >=3.0.0 <4.0.0.
   - No cambia la implementación web.

4. Analyzer:
   - Ignora ejemplos, tests y builds internos de plugins locales.
   - No oculta errores de lib/ de los plugins realmente usados.

5. Limpieza:
   - Se incluye LIMPIAR_PLUGINS_LOCALES_021.ps1 para eliminar ejemplos y repositorios
     anidados que VS Code estaba analizando sin formar parte de la app.

6. BOM:
   - Se eliminaron 0 BOM UTF-8 detectados.

Aplicación:
1. Descomprimir encima de F:\SALUMED\CODE\APP
2. Ejecutar:
   Set-ExecutionPolicy -Scope Process Bypass
   .\LIMPIAR_PLUGINS_LOCALES_021.ps1

3. Limpiar:
   cd .\android
   .\gradlew.bat --stop
   cd ..
   flutter clean
   Remove-Item -Recurse -Force .\build -ErrorAction SilentlyContinue
   Remove-Item -Recurse -Force .\.dart_tool -ErrorAction SilentlyContinue
   Remove-Item -Recurse -Force .\android\.gradle -ErrorAction SilentlyContinue
   Remove-Item -Force .\pubspec.lock -ErrorAction SilentlyContinue
   flutter pub get
   flutter analyze
   flutter run -v *> build_021.log

Git:
git status
git add .
git commit -m "fix: clean and modernize local legacy plugins"
git push
