MegaZIP 022 — assets_audio_player nullable + JVM 17

Base:
- Proyecto actualizado salumed_app-main (2).zip.
- Log Pasted text(86).txt.

Causas reales:
1. assets_audio_player/HeadsetManager.kt
   requestedPermissions es nullable con el Android SDK actual.
   Se cambia:
   requestedPermissions.contains(...)
   por:
   requestedPermissions?.contains(...) == true

2. audio_session
   Java compila a 17 y Kotlin estaba forzado a 11.
   Se cambia el jvmTarget global de módulos Kotlin Android a JVM 17.

No se modifica:
- código Dart
- diseño
- audio público/API Dart
- Mercado Pago o PayPal
- DatePicker o timelines
- applicationId

Aplicación:
1. Descomprimir encima de F:\SALUMED\CODE\APP
2. Ejecutar:

cd .\android
.\gradlew.bat --stop
cd ..

flutter clean
Remove-Item -Recurse -Force .\build -ErrorAction SilentlyContinue
Remove-Item -Recurse -Force .\.dart_tool -ErrorAction SilentlyContinue
Remove-Item -Recurse -Force .\android\.gradle -ErrorAction SilentlyContinue

flutter pub get
flutter build apk --debug -v *> build_022.log

Git:
git status
git add .
git commit -m "fix: align Android Kotlin JVM and nullable audio permissions"
git push
