MegaZIP 01S — Font Awesome 11, Date Picker y Timelines

Cambios:
- font_awesome_flutter 11.0.0.
- 215 usos Icon(FontAwesomeIcons...) migrados a FaIcon(...).
- 11 iconos del menú administrativo usan .data porque ese menú espera IconData.
- flutter_datetime_picker 1.5.1 se copia localmente y se corrige el conflicto DatePickerTheme.
- timelines 0.1.0 se copia localmente:
  hashValues -> Object.hash
  hashList -> Object.hashAll

No se cambian nombres, colores, tamaños, posiciones, callbacks ni diseño.

Aplicación:
Set-ExecutionPolicy -Scope Process Bypass
.\INSTALAR_PLUGINS_HEREDADOS_01S.ps1

cd .\android
.\gradlew.bat --stop
cd ..
flutter clean
Remove-Item -Recurse -Force .\.dart_tool -ErrorAction SilentlyContinue
Remove-Item -Force .\pubspec.lock -ErrorAction SilentlyContinue
flutter pub get
flutter run

Git:
git status
git add .
git commit -m "fix: migrate font awesome and vendor legacy UI plugins"
git push
