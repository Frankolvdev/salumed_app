MegaZIP 027 — flutter_phone_direct_caller en JVM 1.8

Base exacta:
- salumed_app-main (6).zip

Error confirmado:
:flutter_phone_direct_caller:compileDebugKotlin
Java target = 1.8
Kotlin target = 11

Corrección:
Se cambia la selección JVM a un when por módulo.

JVM 1.8:
- flutter_phone_direct_caller

JVM 17:
- app
- assets_audio_player
- audio_session
- audioplayers_android
- device_info_plus
- flutter_custom_tabs_android

JVM 11:
- file_picker
- demás plugins Android heredados que compilan Java 11

No se modifica:
- código Dart
- diseño
- pagos
- API de audio
- plugins locales
- applicationId
- compileSdk
- targetSdk

Aplicación:
1. Descomprimir encima de:
   F:\SALUMED\CODE\APP

2. Limpiar y compilar:

cd .\android
.\gradlew.bat --stop
cd ..

flutter clean
Remove-Item -Recurse -Force .\build -ErrorAction SilentlyContinue
Remove-Item -Recurse -Force .\.dart_tool -ErrorAction SilentlyContinue
Remove-Item -Recurse -Force .\android\.gradle -ErrorAction SilentlyContinue

flutter pub get
flutter build apk --debug -v *> build_027.log

Git:
git status
git add .
git commit -m "fix: align direct caller Kotlin target to JVM 1.8"
git push
